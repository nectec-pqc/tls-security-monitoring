#show title: set align(center)
#set document(
  title: [
    ผลการตรวจสอบ

    การตั้งค่าความปลอดภัยในการเข้ารหัสบนเครือข่าย

    ผ่านชุดเครื่องมือ `tlssec`

    // TODO: Retrieve tlssec version and datetime of the scan from data directory
    //#datetime(year: 2025, month: 6, day: 25).display()
  ]
)

#title()
#pagebreak()
